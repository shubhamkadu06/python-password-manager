
# 🔐 Python Password Manager

A **secure password manager** built using **Python, Tkinter GUI**, and **Cryptography (Fernet encryption)**.

It allows you to:
- Generate random passwords (Low / Medium / Strong)
- Save credentials securely (encrypted in `info.txt`)
- View all saved passwords (after decryption)
- Copy passwords to clipboard easily

---

## 🚀 Features
✅ Tkinter GUI interface  
✅ Strong encryption using `cryptography.fernet`  
✅ Copy password to clipboard  
✅ Save and view encrypted passwords  
✅ Custom password length and strength  

---

## 🧰 Requirements

Install all dependencies using:

```bash
pip install -r requirements.txt
